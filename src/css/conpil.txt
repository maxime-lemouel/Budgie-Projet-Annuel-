// Pour compiler le CSS d'une page, il faut copier-coller la page concernée
// ou créer le script avec les composants utilisés dans la page
// et l'ajouter à la fin de ce fichier pour pouvoir la recompiler.

// Dans le fichier main.scss, avec le watcher activé.
// Pour activer le watcher, il faut faire dans le terminal :
// npm run watch

// Une fois les fichiers components, partials et main.scss sauvegardés en cas de modification,
// un message informe que les fichiers ont bien été compilés. En cas d'erreur, les lire et les corriger.

// Le fichier compilé se trouve dans dist/css, nommé main.css.

// ============================================================
// APP — Frontoffice (dashboard, comptes, dépenses, contact)
// Compilé : dist/css/app.css
// ============================================================
@use "partials/globals";
@use "partials/variables" as v;
@use "partials/mixins"    as m;
@use "partials/functions" as f;

@use "components/button";
@use "components/sidebar";
@use "components/app_layout";
@use "components/card";

@use "components/badge";
@use "components/form";
@use "components/alert";
@use "components/data_table";
@use "components/modal";

body {
  font-family: v.$font-family;
  color: var(--black);
  background: var(--grey-50);

  @media (prefers-color-scheme: dark) {
    background: var(--grey-950);
    color: var(--white);
  }
}

// ============================================================
// ADMIN — Backoffice
// Compilé : dist/css/admin.css
// ============================================================
@use "partials/globals";
@use "partials/variables" as v;
@use "partials/mixins"    as m;
@use "partials/functions" as f;

@use "components/button";
@use "components/sidebar";
@use "components/app_layout";
@use "components/card";

@use "components/badge";
@use "components/form";
@use "components/alert";
@use "components/data_table";
@use "components/modal";
@use "components/admin";

body {
  font-family: v.$font-family;
  color: var(--black);
  background: var(--grey-50);

  @media (prefers-color-scheme: dark) {
    background: var(--grey-950);
    color: var(--white);
  }
}

// ============================================================
// PAGES AUTH — Login / Register / Forgot / Reset / Validate
// Compilé : dist/css/main.css
// ============================================================
@use "partials/globals";
@use "partials/variables" as v;
@use "partials/mixins"    as m;
@use "partials/functions" as f;

@use "components/card";
@use "components/button";
@use "components/form";
@use "components/alert";
@use "components/logo";
@use "components/link";

body {
  @include m.auth-page;
  background: var(--slate);
  padding: f.rem(20);
}

.page-title {
  font-size: v.$font-size-xl;
  font-weight: 700;
}

.page-subtitle,
.page-description {
  font-size: v.$font-size-base;
  color: var(--grey);
  margin-top: 4px;
}

.page-description { line-height: 1.5; }

@media (prefers-color-scheme: dark) {
  body {
    background: var(--slate-950);
    color: var(--white);
  }

  .page-title     { color: var(--white); }
  .page-subtitle,
  .page-description { color: var(--grey-500); }
}